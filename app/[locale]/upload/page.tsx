/**
 * This file exposes the public upload page on localized routes.
 * It keeps the existing upload implementation as the single source of truth.
 */
export { metadata } from "../../upload/page";
export { default } from "../../upload/page";
